# dotfiles

My dotfiles

# Repository Structure
```
.
├── LICENSE
├── README.md
├── bin
│   ├── brew.sh                 <- Install brew packages
│   ├── docker.sh               <- Pull docker images
│   ├── install.sh              <- Install build tools (Homebrew etc)
│   └── oh_my_zsh.sh            <- Innstall and config ZSH
├── dotfiles.code-workspace
├── gpg                         <- GPG settings
│   └── macos
│       ├── gpg-agent.conf
│       └── gpg.conf
└── vscode                      <- VS Code settings
    └── settings.json
```